import React from "react";

import "./notFoundPage.css";

export default function NotFoundPage() {
    return (
        <main className="nfp19Wrapper">
            <h4>Looks like you've entered the address wrong. :(</h4>
        </main>
    );
}
